package practice

/**
 * split array recursively into sorted sub array, and merge it.
 */
fun mergeSort(a: List<Int>): List<Int> {

    return a
}

/**
 * find a pivot p, and split the array recursively
 * swap left and right part by compare it with a[p]
 */
fun quickSort(a: List<Int>): List<Int> {
    return a
}
